<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title><%= post.title %> | The Backseat</title>

    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Merriweather:300,300italic,400,400italic,700,700italic">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/stylesheets/base.css">
    <link rel="stylesheet" href="/stylesheets/post.css">
  </head>
  <body>
    <% include partials/header %>

    <div class="story">
      <div class="container-fluid">
        <div class="row">
          <div class="col-sm-8 col-sm-offset-2">
            <div class="post">
              <h1 class="title"><%= post.title %></h1>
              <% if (post.thumbnailUrl) { %>
                <img src="<%= post.thumbnailUrl %>" class="img-responsive">
              <% } %>
              <p class="byline"><strong><%= post.author.name %> &middot; <%= post.date %></strong></p>
              <div class="body"><%- post.content %></div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="more">
      <div class="container-fluid">
        <div class="row">
          <div class="col-sm-7">
            <div class="recent">
              <h2 class="page-header">Latest Posts</h2>
              <% for (i = 0; i < recentPosts.length; i++) { %>
                <div class="recent-post <%= recentPosts[i].thumbnailUrl ? 'featured-image' : '' %>">
                  <% if (recentPosts[i].thumbnailUrl) { %>
                    <a href="<%= recentPosts[i].url %>"><img src="<%= recentPosts[i].thumbnailUrl %>" class="img-responsive"></a>
                  <% } %>
                  <a href="<%= recentPosts[i].url %>"><h4><%= recentPosts[i].title %></h4></a>
                  <p>by <%= recentPosts[i].author.name %></p>
                </div>
              <% } %>
            </div>
          </div>
          <div class="col-sm-5">
            <% if (mainFeaturedPost) { %>
              <div class="main-feature">
                <a href="<%= mainFeaturedPost.url %>"><img src="<%= mainFeaturedPost.thumbnailUrl %>" class="img-responsive"></a>
                <a href="<%= mainFeaturedPost.url %>"><h3><%= mainFeaturedPost.title %></h3></a>
                <p>by <%= mainFeaturedPost.author.name %></p>
              </div>
            <% } %>
            <% if (featuredPosts) { %>
              <div class="features">  
                <div class="row feature">
                  <div class="col-xs-4">
                    <a href="<%= featuredPosts[0].url %>"><img src="<%= featuredPosts[0].thumbnailUrl %>" class="img-responsive"></a>
                  </div>
                  <div class="col-xs-8">
                    <a href="<%= featuredPosts[0].url %>"><h3><%= featuredPosts[0].title %></h3></a>
                    <p>by <%= featuredPosts[0].author.name %></p>
                  </div>
                </div>
                <% if (featuredPosts.length > 1) { %>
                  <div class="row feature">
                    <div class="col-xs-4">
                      <a href="<%= featuredPosts[1].url %>"><img src="<%= featuredPosts[1].thumbnailUrl %>" class="img-responsive"></a>
                    </div>
                    <div class="col-xs-8">
                      <a href="<%= featuredPosts[1].url %>"><h3><%= featuredPosts[1].title %></h3></a>
                      <p>by <%= featuredPosts[1].author.name %></p>
                    </div>
                  </div>
                <% } %>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>

    <% include partials/footer %>
  </body>
</html>