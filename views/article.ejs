<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title><%= post.title %> | The Backseat</title>

    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Merriweather:300,300italic,400,400italic,700,700italic">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/stylesheets/base.css">
    <link rel="stylesheet" href="/stylesheets/header.css">
    <link rel="stylesheet" href="/stylesheets/article.css">
  </head>
  <body>
    <% include partials/header %>

    <div class="content">
      <div class="container-fluid">
        <div class="row">
          <div class="col-sm-8">
            <div class="post">
              <h1 class="title"><%= post.title %></h1>
              <p class="byline"><%= post.author.name %> &middot; <%= post.date %></p>
              <div class="body"><%- post.content %></div>
            </div>
          </div>
          <div class="col-sm-4">
            <h2 class="featured-posts">Featured Posts</h2>
            <% if (mainFeaturedPost) { %>
              <div class="main-feature">
                <img src="<%= mainFeaturedPost.thumbnailUrl %>" class="img-responsive">
                <a href="<%= mainFeaturedPost.url %>"><h2><%= mainFeaturedPost.title %></h2></a>
                <p>by <%= mainFeaturedPost.author.name %></p>
              </div>
            <% } %>
            <% if (featuredPosts) { %>
              <div class="features">  
                <div class="row feature">
                  <div class="col-xs-4">
                    <img src="<%= featuredPosts[0].thumbnailUrl %>" class="img-responsive">
                  </div>
                  <div class="col-xs-8">
                    <a href="<%= featuredPosts[0].url %>"><h2><%= featuredPosts[0].title %></h2></a>
                    <p>by <%= featuredPosts[0].author.name %></p>
                  </div>
                </div>
                <% if (featuredPosts.length > 1) { %>
                  <div class="row feature">
                    <div class="col-xs-4">
                      <img src="<%= featuredPosts[1].thumbnailUrl %>" class="img-responsive">
                    </div>
                    <div class="col-xs-8">
                      <a href="<%= featuredPosts[1].url %>"><h2><%= featuredPosts[1].title %></h2></a>
                      <p>by <%= featuredPosts[1].author.name %></p>
                    </div>
                  </div>
                <% } %>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>